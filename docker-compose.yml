services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: cluster-7
    restart: unless-stopped
    working_dir: /var/www/html
    ports:
      - "${APP_PORT}:80"
    env_file:
      - .env
